{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Gateway to route requests to microservices",
  "main": "dist/index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development ts-node-dev --respawn src/index.ts",
    "build": "tsup",
    "start": "cross-env NODE_ENV=production node dist/index.js"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
